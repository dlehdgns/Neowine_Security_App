<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.neowine_security_app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Self_demo_Sha"></activity>
        <activity android:name=".GidActivity" />
        <activity android:name=".serial_get_activity" />
        <activity android:name=".Private_key_activity" />
        <activity android:name=".TRNG_Activity" />
        <activity android:name=".ElgamalActivity" />
        <activity android:name=".EcdhActivity" />
        <activity android:name=".EccActivity" />
        <activity android:name=".AesActivity" />
        <activity android:name=".ShaActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>